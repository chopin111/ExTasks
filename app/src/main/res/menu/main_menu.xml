<?xml version="1.0" encoding="utf-8"?>

<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- icon -->
    <item
        android:id="@+id/menu_choose_tabs"
        android:showAsAction="always"
        android:title="Choose tabs"
        android:icon="@drawable/plus"
        />
    <item
        android:id="@+id/menu_login_trello"
        android:showAsAction="ifRoom"
        android:title="Login Trello"
        android:visible="true"/>

    <item
        android:id="@+id/menu_login_todoist"
        android:showAsAction="ifRoom"
        android:title="Login Todoist"
        android:visible="true"/>

    <item
        android:id="@+id/menu_refresh"
        android:icon="@drawable/ic_menu_refresh_holo_light"
        android:showAsAction="ifRoom"
        android:title="@string/action_label_refresh"/>

    <item android:id="@+id/add_note"
        android:title="Add Note"
        android:showAsAction="never"
        />

</menu>
